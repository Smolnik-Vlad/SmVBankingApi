from django.http import HttpResponse
from django.urls import path
from django.views import View

# Create your views here.

urlpatterns = [
    # path('answer/', lambda request: View.as_view(lambda req: HttpResponse("yes"))),
]
